import { useState } from 'react'; import { Link, useNavigate } from 'react-router-dom'; import { AxiosError } from 'axios'; import { register } from '../api/auth'; import { useToast } from '../components/Toast';
export function RegisterPage() { const nav=useNavigate(), toast=useToast(), [form,setForm]=useState({username:'',email:'',password:''}), [busy,setBusy]=useState(false); const submit=async(e:React.FormEvent)=>{e.preventDefault();setBusy(true);try{await register(form);toast('Account created — sign in to begin.');nav('/login');}catch(e){toast((e as AxiosError<{detail?:string}>).response?.data?.detail || 'Could not create account.');}finally{setBusy(false)}}; return <div className="auth-page"><Link className="wordmark" to="/">reelroom</Link><form className="auth-card" onSubmit={submit}><p className="eyebrow">START YOUR DIARY</p><h1>A place for your films.</h1><label>Username<input required minLength={3} value={form.username} onChange={e=>setForm({...form,username:e.target.value})}/></label><label>Email<input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>Password<input required minLength={6} type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label><button className="button full" disabled={busy}>{busy?'Creating…':'Create account'}</button><p className="auth-foot">Already have an account? <Link to="/login">Sign in</Link></p></form></div>; }
