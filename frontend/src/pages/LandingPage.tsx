import { ArrowRight, Film, Star } from 'lucide-react'; import { Link } from 'react-router-dom';
const titles = ['After midnight', 'A second sun', 'The long take', 'Soft focus'];
export function LandingPage() { return <div className="landing"><header><Link className="wordmark" to="/"><Film size={21}/> reelroom</Link><div><Link to="/login" className="text-link">Log in</Link><Link to="/register" className="button small">Join free</Link></div></header><section className="hero"><div><p className="eyebrow">YOUR PERSONAL CINEMA</p><h1>Every film leaves<br/><em>something behind.</em></h1><p className="lede">Keep a living record of what moved you: the films you watched, the ratings you gave, and the thoughts worth returning to.</p><div className="hero-actions"><Link to="/register" className="button">Start your diary <ArrowRight size={17}/></Link><Link to="/search" className="text-link">Explore films</Link></div><div className="micro"><Star size={14} fill="currentColor"/> Rate, review, remember</div></div><div className="hero-posters">{titles.map((title, i) => <div key={title} className={`hero-poster p${i}`}><span>{title}</span></div>)}</div></section><footer>Made for films that stay with you.</footer></div>; }
