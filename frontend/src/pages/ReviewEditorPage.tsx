import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query'; import { AxiosError } from 'axios'; import { useState } from 'react'; import { Link, useNavigate, useParams } from 'react-router-dom'; import { getMovie } from '../api/movies'; import { createReview, getMyReviews, updateReview } from '../api/reviews'; import { movieTitle } from '../api/types'; import { RatingPicker } from '../components/Rating'; import { useToast } from '../components/Toast';
export function ReviewEditorPage(){const {imdbId=''}=useParams(),nav=useNavigate(),toast=useToast(),qc=useQueryClient(); const movie=useQuery({queryKey:['movie',imdbId],queryFn:()=>getMovie(imdbId)});const mine=useQuery({queryKey:['my-review',imdbId],queryFn:()=>getMyReviews(imdbId)});const existing=mine.data?.[0];const [rating,setRating]=useState(3.5),[text,setText]=useState(''),[spoiler,setSpoiler]=useState(false);const mut=useMutation({mutationFn:()=>existing?updateReview({omdb_id:imdbId,rating,review:text,spoiler}):createReview({omdb_id:imdbId,rating,review:text,spoiler}),onSuccess:()=>{qc.invalidateQueries({queryKey:['my-reviews']});qc.invalidateQueries({queryKey:['movie-reviews',imdbId]});toast(existing?'Review updated.':'Review added to your diary.');nav(`/film/${imdbId}`)},onError:(e)=>toast((e as AxiosError<{detail?:string}>).response?.data?.detail||'Could not save review.')});const initialize=()=>{if(existing&&!text){setRating(existing.rating);setText(existing.review||'');setSpoiler(!!existing.spoiler)}}; if(existing) initialize(); return <section className="page editor"><Link className="back" to={`/film/${imdbId}`}>← Back to film</Link><p className="eyebrow">{existing?'EDIT REVIEW':'WRITE A REVIEW'}</p><h1>{movie.data?movieTitle(movie.data):'Your thoughts'}</h1><RatingPicker value={rating} onChange={setRating}/><label className="review-text">Your note<textarea value={text} onChange={e=>setText(e.target.value)} maxLength={3000} placeholder="What did it leave you thinking about?"/></label><label className="toggle"><input type="checkbox" checked={spoiler} onChange={e=>setSpoiler(e.target.checked)}/> Contains spoilers</label><button className="button" onClick={()=>mut.mutate()} disabled={mut.isPending}>{mut.isPending?'Saving…':existing?'Save changes':'Publish review'}</button></section>}
