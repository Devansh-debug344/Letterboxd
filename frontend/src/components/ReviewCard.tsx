import { useState } from 'react'; import { Eye, Star } from 'lucide-react'; import type { Review } from '../api/types';
export function ReviewCard({ review }: { review: Review }) { const [shown, setShown] = useState(!review.spoiler); return <article className="review-card"><div className="review-meta"><span className="avatar">{review.user_name?.slice(0, 1).toUpperCase()}</span><div><b>{review.user_name}</b><small>{new Date(review.updated_at).toLocaleDateString(undefined, { month: 'short', year: 'numeric' })}</small></div><span className="score"><Star size={14} fill="currentColor" /> {review.rating.toFixed(1)}</span></div>{review.spoiler && !shown ? <button className="spoiler" onClick={() => setShown(true)}><Eye size={16} /> Contains spoilers — show review</button> : review.review && <p>{review.review}</p>}</article>; }
