import { Link } from 'react-router-dom'; import { Star } from 'lucide-react'; import type { Movie } from '../api/types'; import { movieId, moviePoster, movieTitle, movieYear } from '../api/types';
export function PosterCard({ movie, rating, compact = false }: { movie: Movie; rating?: number | null; compact?: boolean }) { const id = movieId(movie); const poster = moviePoster(movie); return <Link className={`poster-card ${compact ? 'compact' : ''}`} to={id ? `/film/${id}` : '/search'}>{poster ? <img src={poster} alt={`${movieTitle(movie)} poster`} loading="lazy" /> : <div className="poster-fallback">{movieTitle(movie)}</div>}<div className="poster-overlay"><strong>{movieTitle(movie)}</strong><span>{movieYear(movie)}{rating != null && <> <Star size={12} fill="currentColor" /> {rating.toFixed(1)}</>}</span></div></Link>; }
export function PosterSkeleton() { return <div className="poster-skeleton" />; }
